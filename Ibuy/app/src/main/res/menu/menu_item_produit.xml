<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/ajout_souhait"
        android:title="Ajouter à la liste des souhaits"
        android:visible="true"
        app:showAsAction="never"></item>
    <item
        android:id="@+id/retirer_souhait"
        android:title="Retirer de la liste des souhaits"
        android:visible="false"
        app:showAsAction="never"></item>
    <item
        android:id="@+id/ajout_panier"
        android:title="Ajouter au panier"
        app:showAsAction="never"></item>
</menu>